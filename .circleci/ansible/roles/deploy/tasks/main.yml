---

- name: "Extract the backend files"
  unarchive:
    src: ~/project/artifact.tar.gz
    dest: ~/

- name: "Load environment variables"
  become_method: sudo
  become_user: root
  become: yes
  shell: |
    ls -al
    echo "NODE_ENV=production" >> ~/artifact/.env
    echo "ENVIRONMENT=production" >> ~/artifact/.env
    echo "TYPEORM_CONNECTION={{ lookup('env', 'TYPEORM_CONNECTION')}}" >> ~/artifact/.env
    echo "TYPEORM_ENTITIES={{ lookup('env', 'TYPEORM_ENTITIES')}}" >> ~/artifact/.env
    echo "TYPEORM_HOST={{ lookup('env', 'TYPEORM_HOST')}}" >> ~/artifact/.env
    echo "TYPEORM_PORT={{ lookup('env', 'TYPEORM_PORT')}}" >> ~/artifact/.env
    echo "TYPEORM_USERNAME={{ lookup('env', 'TYPEORM_USERNAME')}}" >> ~/artifact/.env
    echo "TYPEORM_PASSWORD={{ lookup('env', 'TYPEORM_PASSWORD')}}" >> ~/artifact/.env
    echo "TYPEORM_DATABASE={{ lookup('env', 'TYPEORM_DATABASE')}}" >> ~/artifact/.env
    echo "TYPEORM_MIGRATIONS={{ lookup('env', 'TYPEORM_MIGRATIONS')}}" >> ~/artifact/.env
    echo "TYPEORM_MIGRATIONS_DIR={{ lookup('env', 'TYPEORM_MIGRATIONS_DIR')}}" >> ~/artifact/.env

- name: "Start the app"
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start