---

- name: "Extract the backend files"
  unarchive:
    src: ~/project/artifact.tar.gz
    dest: ~/

- name: "Load environment variables"
  become_method: sudo
  become_user: root
  become: yes
  shell: |
    ls -al
    echo "NODE_ENV=production" >> ~/backend/.env
    echo "ENVIRONMENT=production" >> ~/backend/.env
    echo "TYPEORM_CONNECTION={{ lookup('env', 'TYPEORM_CONNECTION')}}" >> ~/backend/.env
    echo "TYPEORM_ENTITIES={{ lookup('env', 'TYPEORM_ENTITIES')}}" >> ~/backend/.env
    echo "TYPEORM_HOST={{ lookup('env', 'TYPEORM_HOST')}}" >> ~/backend/.env
    echo "TYPEORM_PORT={{ lookup('env', 'TYPEORM_PORT')}}" >> ~/backend/.env
    echo "TYPEORM_USERNAME={{ lookup('env', 'TYPEORM_USERNAME')}}" >> ~/backend/.env
    echo "TYPEORM_PASSWORD={{ lookup('env', 'TYPEORM_PASSWORD')}}" >> ~/backend/.env
    echo "TYPEORM_DATABASE={{ lookup('env', 'TYPEORM_DATABASE')}}" >> ~/backend/.env
    echo "TYPEORM_MIGRATIONS={{ lookup('env', 'TYPEORM_MIGRATIONS')}}" >> ~/backend/.env
    echo "TYPEORM_MIGRATIONS_DIR={{ lookup('env', 'TYPEORM_MIGRATIONS_DIR')}}" >> ~/backend/.env

- name: "Start the app"
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start